import { AdzSearchPipe } from './adz-search.pipe';

describe('AdzSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AdzSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
